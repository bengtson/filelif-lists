    <!-- Day Letter -->
    <span style="float: right; padding-right: 3px; font-weight: <%= if entry["Bold"] do %>bold<% else %>normal<% end %>;" title="<%= entry["Hover"] %>"><%= entry["Letter"] %></span>
